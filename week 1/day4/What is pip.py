# PYTHONS TOOLS

# Built-in (Standard Library)
# Comes with Python automatically
# No installation needed
# Just import and use
# Examples: math, random, datetime, os, json, csv

# Third-Party Packages
# Built by other developers
# Must be installed with pip first
# Then import and use
# Examples: requests, pandas, openai, flask

# What pip is? pip stands for "Pip Installs Packages." It is a command-line tool that comes 
# with Python automatically. You run it in your terminal to download and install packages from PyPI,
#  the Python Package Index, which is a public library of over 500,000 free Python packages.

# The pip Command
You run pip in your terminal, not inside a Python file. The syntax is always the same:

# Install a package
pip install package-name

# Install a specific version
pip install package-name==2.1.0

# See all packages you have installed
pip list

# Check if a specific package is installed
pip show package-name