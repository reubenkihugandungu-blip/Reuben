#  You will build a multi-section dashboard that pulls from three simulated API endpoints, processes the data, 
# and outputs a complete report.Every concept from this week appears: 
# nested JSON, key safety, processing functions, and structured output.