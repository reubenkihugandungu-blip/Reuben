// The DOM (Document Object Model) is a tree of objects that represents every element in an HTML page. 
// The browser builds it when the page loads. JavaScript can read from it, write to it, add new nodes, 
// and remove existing ones. Every change to the DOM updates what the user sees instantly, without a page reload.

// Known example: A tracking board at an SMP workshop. Each row is a client. 
// JavaScript is the staff member standing at the board. When a client's status changes, 
// the staff member walks over and updates that row. The rest of the board stays untouched. 
// That is DOM manipulation: targeted changes to specific elements.
