// Build a complete browser tool that accepts daily inputs, runs a rule-based prediction, 
// generates a coaching message, and displays results in a live UI without a page reload.

// Project: SMP Daily AI Coach (Browser Tool)
// A browser-based tool that accepts sleep, water, and step count data, predicts goal outcome with a confidence score,
//  and generates a coaching message. No server needed. Everything runs client-side in JavaScript.

// Four steps:

// Step 1: Build the prediction engine in JavaScript
// Step 2: Build the coaching message generator
// Step 3: Wire the form inputs and DOM display
// Step 4: Add session history log